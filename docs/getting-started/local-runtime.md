# Local Runtime

ICE Studio runs a local backend runtime written in Python.

This runtime hosts:

- system agents
- knowledge layers
- logging and diagnostics
- LLM and embedding providers

The runtime starts before any project is loaded.
